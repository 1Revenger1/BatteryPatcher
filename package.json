{
  "name": "battery-patcher",
  "version": "1.0.0",
  "description": "Battery Patcher for SMCBatteryManager",
  "main": "./program/Index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "run": "node ./program/Index.js",
    "start": "npm run build --silent && npm run run --silent"
  },
  "repository": "https://github.com/1Revenger1/BatteryPatcher",
  "author": "1Revenger1",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^13.13.0"
  },
  "dependencies": {
    "@types/plist": "^3.0.2",
    "chalk": "^4.0.0",
    "plist": "^3.0.1",
    "typescript": "^3.8.3"
  }
}
